// This is a backup of the original SuperMenu component
// It's not being used in the current design but kept for reference
"use client"

interface SuperMenuProps {
  scrolled?: boolean
}

export function SuperMenuBackup({ scrolled = false }: SuperMenuProps) {
  // Original SuperMenu code kept for reference
  // This component is not being used in the current design
}

